# Review Journal

I treated `planwright` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its databases focus without claiming live deployment or external usage.

## Cases

- `baseline`: `index fit`, score 209, lane `ship`
- `stress`: `join width`, score 199, lane `ship`
- `edge`: `constraint risk`, score 189, lane `ship`
- `recovery`: `plan drift`, score 179, lane `ship`
- `stale`: `index fit`, score 223, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
